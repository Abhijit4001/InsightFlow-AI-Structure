{
  "name": "insightflow-ai-dashboard",
  "version": "1.0.0",
  "description": "Modern AI-powered analytics dashboard with FastAPI, Chart.js, and Groq.",
  "private": true,
  "scripts": {
    "dev": "python -m uvicorn app:app --reload --host 127.0.0.1 --port 8000",
    "start": "python -m uvicorn app:app --host 0.0.0.0 --port 8000"
  }
}